<UserControl x:Class="Encrypt_Decrypt.VVM.ViewModel.PolybiusKey"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Encrypt_Decrypt.VVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920" Background="#283044">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
            <TextBlock FontSize="50" VerticalAlignment="Center" HorizontalAlignment="Center"
                   Margin="0,0,0,50">Enter key</TextBlock>
        <Button Click="PolybiusKey_Click" Grid.Row="0" Width="100" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#EBF5EE" Margin="0,100,0,0" Name="SubmitCaesarsKey">Submit</Button>
        
        <Grid Name="PolybiusSquare" Margin="100,100,100,100" Grid.Row="2">
            
        </Grid>
        
    </Grid>
</UserControl>
